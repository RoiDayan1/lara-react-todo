{
    "name": "@roid/server",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start": "php artisan serve --port=3005",
        "first": "composer install && touch database/database.sqlite && php artisan migrate:fresh --seed --force",
        "migrate": "php artisan migrate",
        "start:prod": ""
    },
    "dependencies": {},
    "devDependencies": {
        "axios": "^0.21.0",
        "laravel-vite-plugin": "^0.6.0",
        "lodash": "^4.17.20",
        "postcss": "^8.1.14",
        "vite": "^3.0.0"
    }
}
